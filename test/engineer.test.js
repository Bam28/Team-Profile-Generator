const Engineer = require('../lib/engineer')
const Employee = require('../lib/employee')

describe('Engineer', () => {
  it('Get name of Engineer', () => {
    const name = name;
    Engineer.getName() = name

    expect(Engineer.getName).toEqual(name)
  });
});